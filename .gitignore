.superpowers/
node_modules/
dist/
*.tsbuildinfo
**/.spider/scratch/
# Per-machine spider state. config.json holds personal toggles (ui.footer,
# models.defaults) and the exec.enforce safety switch -- committing
# "exec.enforce": false would silently disable bash enforcement for every clone.
**/.spider/config.json

# Legacy pi-subagents run artifacts (superseded by spider). Machine-local
# prompt/output dumps -- purged from history, never re-commit.
.pi-subagents/
**/.spider/project.db*
**/.spider/logs/
.DS_Store

# local pi dev extension discovery (dev loop)
.pi/
.pi-subagents/
.scratch/
coverage/
